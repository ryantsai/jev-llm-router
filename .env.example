# Documentation only: the application does not automatically load .env files.
# Set these in your shell, service manager, or Docker Compose environment.
JEV_ROUTER_TOKEN=replace-with-a-new-random-local-token
JEV_UPSTREAM_API_KEY=replace-with-your-upstream-api-credential
